server:
  port: 8762

spring:
  application:
    name: serverfirst
  cloud:
      config:
        enabled: true
        discovery:
          enabled: true
          service-id: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/