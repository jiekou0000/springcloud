server:
  port: 8763

spring:
  application:
    name: serverfirst
  cloud:
      config:
        enabled: true
        discovery:
          enabled: true
          service-id: config

eureka:
  client:
    serviceUrl:
      defaultZone: http://peer0:8760/eureka/